{
  "name": "paper-bridge-mvp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:vision": "node scripts/prepare-vision-assets.mjs",
    "predev": "npm run prepare:vision",
    "dev": "node --env-file-if-exists=.env.local server.mjs",
    "prestart": "npm run prepare:vision",
    "start": "node --env-file-if-exists=.env.local server.mjs",
    "prebuild": "npm run prepare:vision",
    "build": "node scripts/build.mjs",
    "test": "node tests/run.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35",
    "iconv-lite": "^0.7.0",
    "sharp": "^0.35.3"
  }
}
