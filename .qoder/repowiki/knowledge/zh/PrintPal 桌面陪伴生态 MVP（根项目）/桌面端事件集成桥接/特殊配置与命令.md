日志桥接以独立进程启动：`python -m integrations.desktop_bot.log_event_bridge --log <path> [--endpoint http://...] [--timeout 3.0] [--from-start]`；Web 转发器需复制进 desktop_bot 仓库并在 `EventCache.append()` 后调用 `await forwarder.send(event)`。