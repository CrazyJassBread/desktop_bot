通过 `python -m app` 或 `python app/main.py` 启动，支持 `run`/`test` 两种模式；可通过 `--config config.yaml` 指定配置、`--session` 覆盖会话 ID、`--audio-only`/`--vision-only` 单通道运行、`--audio-host/--audio-port/--vision-host/--vision-port` 覆盖监听地址端口、`--scale` 控制测试窗口缩放。