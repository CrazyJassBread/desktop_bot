本模块为纯文档仓库，按主题分层组织：`docs/` 存放面向运行时的工程文档（如 `app-pipeline.md` 描述 Python 感知 Pipeline 的启动、并发与事件流；`desktop-bot-integration.md` 定义与 desktop_bot 的 WebSocket/HTTP 联动协议；`FRONTEND_HANDOFF.md` 给出前端交接规范与参考实现路径；`perception-runtime.md` 与 `AI_Hub_OS_项目全景与商业规划.md` 覆盖感知运行时与商业定位）。`AI_Hardware_Community_Project/` 按产品→架构→前端→后端→数据库→AI Agent→硬件→服务器→API→开发日志的编号目录划分，每个子目录对应一个设计域，其中 `09_API/openapi.yaml` 作为 MVP 核心接口的单一事实源，`05_Database/schema.sql` 提供可执行的 DDL 基线，`README.md` 充当整个文档集的导航与版本索引。依赖方向单向向下：高层文档引用底层契约文件，不反向依赖代码实现。