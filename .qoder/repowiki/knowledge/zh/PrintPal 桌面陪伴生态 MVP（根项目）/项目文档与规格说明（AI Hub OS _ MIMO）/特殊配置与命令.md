本地参考前端可通过 `npm install && copy .env.example .env.local && npm run dev` 启动于 `http://127.0.0.1:18000`；Python 感知服务通过 `python -m app`（或 `--audio-only`/`--vision-only`/`test` 模式）启动，参数覆盖 `config.yaml` 中的 audio/vision/api 等 section。