各脚本均为独立可执行入口，直接 `python scripts/xxx.py` 或 `python tools/printer_test.py` 运行；`vision_live.py` 需先安装 OpenCV (`requirements.txt`) 且要求 `config.yaml` 中 `vision.enabled=true`；默认 ESP32 地址为 `10.76.0.126`，可通过 `--esp-ip` 覆盖。