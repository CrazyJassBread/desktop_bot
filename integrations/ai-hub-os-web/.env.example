# Use the same secret in the Python App environment.
AI_HUB_BRIDGE_TOKEN=replace-with-a-long-random-secret

# SQLite database and Web server.
DATABASE_PATH=./data/letters.sqlite
PORT=18000
HOST=127.0.0.1
