# Integrations

此目录包含与桌面 Bot 配套、但不运行在 ESP32 固件中的应用。

## `ai-hub-os-web`

精简的用户信件空间，提供：

- 用户注册、登录和安全会话；
- SQLite 用户与信件持久化；
- 当前用户的收件箱和已发送信件；
- App 语音写信完成后的双向信箱同步。

硬件与语音运行时仍保留在仓库根目录。网页端只管理账号与信件，不再承载社区、
匹配、照片、浏览器语音、设备控制或打印功能。
