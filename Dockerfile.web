FROM node:22-bookworm-slim

ENV NODE_ENV=production \
    HOST=0.0.0.0 \
    PORT=8080 \
    DATABASE_PATH=/data/letters.sqlite

WORKDIR /app
COPY integrations/ai-hub-os-web ./
RUN mkdir -p /data

EXPOSE 8080
CMD ["npm", "start"]
